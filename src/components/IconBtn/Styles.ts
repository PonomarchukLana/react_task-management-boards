import styled from 'styled-components';

export const IconWrapp = styled.button`
  border: none;
  background: transparent;
  cursor: pointer;
  transition: transform ease-in-out .2s;

  &:hover {
    transform: scale(1.1);
  }
`;
